import { EateryCategory } from '../types';

export const MODEL_MAP: Record<EateryCategory, string> = {
  restaurant: '/models/shophouse_A_opt.glb',
  dessert: '/models/shophouse_B_opt.glb',
  hawker: '/models/hawker_stall_opt.glb',
};
