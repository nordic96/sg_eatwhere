import { EateryCategory } from '../types';

export const MODEL_MAP: Record<EateryCategory, string> = {
  restaurant: '/models/shophouse_d.glb',
  dessert: '/models/shophouse_d.glb',
  hawker: '/models/hawker_d.glb',
};
